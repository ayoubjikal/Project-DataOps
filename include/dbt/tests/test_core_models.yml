version: 2

models:
  - name: dim_product
    columns:
      - name: product_key
        tests:
          - not_null
          - unique

  - name: dim_customer
    columns:
      - name: customer_key
        tests:
          - not_null
          - unique

  - name: dim_date
    columns:
      - name: date_key
        tests:
          - not_null
          - unique

  - name: fact_sales
    columns:
      - name: line_id
        tests:
          - not_null
          - unique
      - name: invoice_date
        tests:
          - not_null
